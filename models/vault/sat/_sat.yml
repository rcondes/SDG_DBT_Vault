version: 2

models:
  - name: sat_customer
    config:
      tags: ['sat']
    description: "Satellite de customer: atributos y su historia. Inserta solo si cambian atributos."
    columns:
      - name: sat_key
        tests: [not_null, unique]
      - name: parent_hub_key
        tests: [not_null]
      - name: customer_id
        tests: [not_null]
      - name: attribute_hash
      - name: load_dttm
      - name: version
      - name: record_source

  - name: sat_orders
    config:
      tags: ['sat']
    description: "Satellite de orders: atributos y su historia. Inserta solo si cambian atributos."
    columns:
      - name: sat_key
        tests: [not_null, unique]
      - name: parent_hub_key
        tests: [not_null]
      - name: order_id
        tests: [not_null]
      - name: attribute_hash
      - name: load_dttm
      - name: version
      - name: record_source

  - name: sat_part
    config:
      tags: ['sat']
    description: "Satellite de part: atributos y su historia. Inserta solo si cambian atributos."
    columns:
      - name: sat_key
        tests: [not_null, unique]
      - name: parent_hub_key
        tests: [not_null]
      - name: part_id
        tests: [not_null]
      - name: attribute_hash
      - name: load_dttm
      - name: version
      - name: record_source

  - name: sat_supplier
    config:
      tags: ['sat']
    description: "Satellite de supplier: atributos y su historia. Inserta solo si cambian atributos."
    columns:
      - name: sat_key
        tests: [not_null, unique]
      - name: parent_hub_key
        tests: [not_null]
      - name: supplier_id
        tests: [not_null]
      - name: attribute_hash
      - name: load_dttm
      - name: version
      - name: record_source

  - name: sat_nation
    config:
      tags: ['sat']
    description: "Satellite de nation: atributos y su historia."
    columns:
      - name: sat_key
        tests: [not_null, unique]
      - name: parent_hub_key
        tests: [not_null]
      - name: nation_id
        tests: [not_null]
      - name: attribute_hash
      - name: load_dttm
      - name: version
      - name: record_source

  - name: sat_region
    config:
      tags: ['sat']
    description: "Satellite de region: atributos y su historia."
    columns:
      - name: sat_key
        tests: [not_null, unique]
      - name: parent_hub_key
        tests: [not_null]
      - name: region_id
        tests: [not_null]
      - name: attribute_hash
      - name: load_dttm
      - name: version
      - name: record_source